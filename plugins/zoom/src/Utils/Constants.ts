/**
 * Zoom plugin constants
 */

/**
 * Minimum zoom level
 */
export const minZoom = 0.1;

/**
 * Maximum zoom level
 */
export const maxZoom = 5;

/**
 * Zoom in factor (mouse wheel)
 */
export const zoomInFactor = 1.05;

/**
 * Zoom out factor (mouse wheel)
 */
export const zoomOutFactor = 0.95;

/**
 * Gesture zoom factor (trackpad/touch)
 */
export const zoomGestureFactor = 0.25;

/**
 * Number of touch points required for pinch zoom
 */
export const touchPointsCount = 2;

/**
 * Index of first touch point
 */
export const touchPointIndexFirst = 0;

/**
 * Index of second touch point
 */
export const touchPointIndexSecond = 1;

/**
 * Divisor for calculating touch center point
 */
export const touchCenterDivisor = 2;

/**
 * Initial touch distance value
 */
export const initialTouchDistance = 0;
