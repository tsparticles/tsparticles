# tsParticles 4.3.0 — Master Coordination Plan

## Overview

This plan coordinates **7 features** (A, C, D, E, F, G, H) targeting the `4.3.0` release.
Each feature has its own detailed handover plan. This document manages sequencing, dependencies,
conflict areas, and release readiness.

---

## Hard Rule: Master Plan First, Feature Plans After

All validation steps defined in **this** document (see "Step 0: Baseline Validation" below) **must be executed and passing before any feature-specific plan** (i.e. before `BACKGROUND_CANVAS_PLAN.md`, `FLUID_INTERACTION_PLAN.md`, `GIF_SHAPE_PLAN.md`) is worked on. Even if new feature plans are added to the 4.3.0 scope mid-cycle, run this master plan's checks first, then proceed to the feature plan's steps.

This rule exists because the 4.2.0 release was burned by a `workspace:*` → hardcoded version regression discovered only at CI publish time. Running the master gate first establishes a baseline so feature work doesn't mask or introduce regressions.

---

## Step 0: Baseline Validation (run BEFORE any feature work)

Before touching any feature plan, execute these checks on the current `main` state:

### 0.1 — Workspace `@tsparticles/` deps use `workspace:*`

✅ **Risolto nella 4.2.1** — ora automatico via `scripts/post-version-fix.js`.

Il comando `pnpm run version:patch` (e gli altri `version:*`) esegue:
1. `lerna version` (bump versioni, genera commit + tag)
2. `pnpm run postversion:fix` → `scripts/post-version-fix.js`
   - ripristina eventuali `workspace:*` riscritti da lerna in versioni hardcoded
   - sincronizza la versione del root `package.json` con `engine/package.json`
   - se ci sono fix: `git commit --amend --no-edit` + spostamento tag

Nota: il controllo manuale via script non serve più, il fix è automatico nello stesso commit del tag.

### 0.2 — `pnpm run build:ci` passes on clean state

```bash
pnpm run build:ci
```

If either check fails, fix the regression first before starting any feature work. Log the fix commit hash in the dashboard.

---

## Progress Dashboard

Checklist riassuntiva di tutte le attività del piano. Da tenere aggiornata durante l'esecuzione.

```
## 4.3.0 Dashboard — 2026-06-21

### C: GIF Shape
- [x] Step 1: scaffold shapes/gif/ (17 files)
- [x] Step 2: copy + adapt GifUtils (10 files)
- [x] Step 3: remove GIF from shape-image (9 files)
- [x] Step 4: add to @tsparticles/all (2 files)
- [x] Step 5: build + verify
- [x] Step 6: demo config updated

### A: Background Canvas (v2 layered redesign)
- [x] V1 phases 0-8 completed (original element + draw implementation)
- [x] Phase 9: types/methods rename (widen element type, #backgroundElement)
- [x] Phase 10: rewrite #drawBackground() — independent element + draw layers
- [x] Phase 11: rewrite #resolveBackgroundElement() — store element ref, no context
- [x] Phase 12: tests updated for v2 semantics
- [x] Phase 13: docs updated for layering model
- [x] Phase 14: bundle impact re-measurement + plugin extraction decision
- [x] Phase 15: validation gate

### H: Fluid Interaction
- [x] Step 0: conventions aligned, naming frozen
- [x] Step 1: package skeleton + noop interactor
- [x] Step 2: options contract + loaders + tests
- [x] Step 3: baseline DDR solver
- [x] Step 4: boundaries + caps + stability
- [x] Step 5: viscosity + pointer stirring
- [ ] Step 6: interoperability validated
- [ ] Step 7: performance + soak passed
- [ ] Step 8: docs + presets finalized
- [ ] Step 9: (optional) plugin orchestration

### D: Background Mask Dynamic
- [ ] Phase 0: Baseline scan + analogia Background Canvas
- [ ] Phase 1: Type contract — estendere IBackgroundMaskCover / BackgroundMaskCover
- [ ] Phase 2: Runtime hook — modificare BackgroundMaskPluginInstance.canvasPaint()
- [ ] Phase 3: Element resolution/cache — #resolveMaskElement()
- [ ] Phase 4: Error/warning handling
- [ ] Phase 5: Tests
- [ ] Phase 6: Bundle impact + docs
- [ ] Phase 7: Validation gate

### E: Draw Layer System
- [ ] Phase 0: DrawLayer enum + types
- [ ] Phase 1: RenderManager refactor (layers replace 11 arrays)
- [ ] Phase 2: Clear integration (layer-aware)
- [ ] Phase 3: Plugin migration (set explicit layers)
- [ ] Phase 4: Tests + docs

### F: Particle Modifier System
- [ ] Phase 0: IParticleModifier interface
- [ ] Phase 1: Particle class (modifier methods, refactor getters, remove bubble)
- [ ] Phase 2: Bubble interactor migration (use modifier API)
- [ ] Phase 3: Verify + cleanup (destroy/reinit, grep engine for bubble refs)

### G: HDR Precision
- [ ] Phase 0: HDR precision constants
- [ ] Phase 1: hslToRgbFloat (no Math.round)
- [ ] Phase 2: HDR color pipeline (getStyleFromHsl, cache precision, random colors)
- [ ] Phase 3: Verify color output + animation smoothness

### Release Gate (4.3.0)
- [x] ~~Step 0.1~~ — Workspace `@tsparticles/` deps use `workspace:*` (✅ automatico via `post-version-fix.js` dalla 4.2.1)
- [x] Step 0.2 — `pnpm run build:ci` passes (461 Nx projects build ok; website docs build failure pre-existing, non-blocking)
- [x] ~~Root `package.json` version matches packages~~ (✅ automatico via `post-version-fix.js` dalla 4.2.1)
- [ ] Integration tests pass
- [ ] Demo server smoke-tested
```

---

## Feature Inventory

| # | Feature                             | Package(s) Affected                                                                                  | Type                   | Plan                                   |
|---|-------------------------------------|------------------------------------------------------------------------------------------------------|------------------------|----------------------------------------|
| D | Background Mask Dynamic Source      | `@tsparticles/plugin-background-mask`                                                                | Plugin enhancement     | `BACKGROUND_MASK_DYNAMIC_PLAN.md`      |
| C | Separate GIF Shape from Image Shape | `@tsparticles/shape-gif` (new), `@tsparticles/shape-image` (modified), `@tsparticles/all` (modified) | Refactor + new package | `GIF_SHAPE_PLAN.md`                    |
| A | Background Canvas layered pipeline  | `@tsparticles/engine`                                                                                | Engine enhancement     | `BACKGROUND_CANVAS_PLAN.md`            |
| E | Draw Layer System                   | `@tsparticles/engine`, `@tsparticles/plugin-background-mask`, `@tsparticles/plugin-trail`            | Engine refactor        | `DRAW_LAYER_SYSTEM_PLAN.md`            |
| F | Particle Modifier System            | `@tsparticles/engine`, `@tsparticles/interaction-external-bubble`                                    | Engine refactor        | `PARTICLE_MODIFIER_PLAN.md`            |
| G | HDR Precision                       | `@tsparticles/engine`                                                                                | Engine enhancement     | `HDR_PRECISION_PLAN.md`                |
| H | Fluid Particle Interaction          | `@tsparticles/interaction-particles-fluid`                                                           | New package            | `FLUID_INTERACTION_PLAN.md`            |

> **Nota:** D ha priorità maggiore di H. Vedi Sequencing Strategy per ordine di esecuzione aggiornato.

---

## Dependency Graph

```
D (BgMask Dynamic)        C (GIF Shape)             A (Background Canvas)     E (Draw Layer System)
  plugin only                shape-image refactor       engine only               engine refactor
  no engine changes          shape-gif (new)            no deps on others         no deps on others
                             all bundle update

F (Particle Modifier)     G (HDR Precision)         H (Fluid Interaction)
  engine + bubble           engine only               new package only
  no deps on others         no deps on others         🕐 under review

All seven are INDEPENDENT — no shared files, no conflicting changes.
They can be implemented in any order or in parallel.
```

### Conflict matrix

| Area | D ∩ C | D ∩ A | D ∩ H | C ∩ A | C ∩ H | A ∩ H | E ∩ F | E ∩ G | F ∩ G | E ∩ A | F ∩ A |
|------|-------|-------|-------|-------|-------|-------|-------|-------|-------|-------|-------|
| Files | None | None | None | None | None | None | None | None | None | **Potentially overlap** (both touch RenderManager) | F removes `bubble` from Particle, A doesn't touch Particle |
| Types | None | D importa `BackgroundDrawContext` | None | None | None | None | None | None | F adds `IParticleModifier` | None | F removes `IBubbleParticleData` from engine types |
| Engine | No | No | No | A modifica engine; C solo tipi | C, H solo tipi | No | E+F toccano engine ma file diversi (RenderManager vs Particle) | E+G toccano engine (RenderManager vs ColorUtils) | F+G toccano engine (Particle vs ColorUtils) | **E rifà initPlugins/drawParticles** che A modifica via `#drawBackground` | No |
| Bundle `all` | Not affected | C touches `bundles/all/` | Not affected | Not affected | Not affected | Not affected | Not affected | Not affected | Not affected | Not affected | Not affected |
| Risk | None | None | None | None | None | None | Low | Low | Low | **Medium** — E ristruttura il pipeline dove A ha appena inserito `#drawBackground` | Low |

**Key conflict: E (Draw Layer System) and A (Background Canvas v2) both modify `RenderManager.ts`.**
- A added `#drawBackground()`, `#backgroundElement`, `#resolveBackgroundElement()`
- E replaces the entire plugin array system with layers and moves background into layers
- **Resolution:** Implement E AFTER A is complete. E absorbs A's background changes into the layer system. A's `#drawBackground()` is split into BackgroundElement/BackgroundDraw layer handlers.
- F modifies `Particle.ts` — no conflict with A or E.
- G modifies `ColorUtils.ts` and `Constants.ts` — no file conflicts with any other feature.

**Verdict:** E has a moderate merge dependency on A (E should come after A). F, G, H are fully parallelizable with everything.

---

## Sequencing Strategy

E has a **moderate merge dependency on A** (both touch RenderManager.ts; E absorbs A's background changes). Other combinations (F, G) are independent.

Recommended execution **with dependency awareness**:

```
Week 1-2:
  Agent D: Background Mask Dynamic (Phases 0-4)
  Agent C: GIF Shape (Steps 1-4)                ✅ done
  Agent A: Background Canvas (Phases 9-11)      ✅ done
  Agent F: Particle Modifier (Phases 0-1)        ← can start early (no deps)
  Agent G: HDR Precision (Phases 0-2)            ← can start early (no deps)
  Agent E: Draw Layer System                     ← WAIT for A to finish Phase 11

Week 2-3:
  Agent D: Background Mask Dynamic (Phases 5-7)
  Agent F: Particle Modifier (Phases 2-3)        ← finishes
  Agent G: HDR Precision (Phases 3-4)            ← finishes
  Agent E: Draw Layer System (Phases 0-4)        ← starts after A is committed

Week 3-4:
  Integration: build all, integration tests, soak tests
  (If H is greenlit): Fluid Interaction (Steps 6-9)
  Release prep: changelog, version bump to 4.3.0
```

If resources are limited, priority order:
1. **Feature C (GIF Shape)** — simplest, lowest risk, unblocks the image shape cleanup ✅
2. **Feature F (Particle Modifier)** — removes engine debt (bubble leak), unlocks cleaner particle API
3. **Feature A (Background Canvas)** — moderate, needs bundle-size measurement gate ✅
4. **Feature G (HDR Precision)** — low risk, small scope (float precision in color pipeline)
5. **Feature E (Draw Layer System)** — medium risk, 3-5 days work, requires A to be done first
6. **Feature D (Background Mask Dynamic)** — pattern già validato, plugin isolato, alto valore wow
7. **Feature H (Fluid Interaction)** — 🕐 in attesa di revisione, lowest priority

---

## Cross-feature Concerns

### Per-Feature Verification (run during/after each feature implementation)

- [ ] **D:** `cover.element` e `cover.draw` accettati in opzioni
- [ ] **D:** Elemento dinamico (canvas/video/img) disegnato come mask prima del composite
- [ ] **D:** Draw callback eseguito ogni frame come mask
- [ ] **D:** Fallback legacy (color/image) invariato quando element/draw non usati
- [ ] **D:** Warning non ripetitivi per selector invalido, draw error, etc.
- [x] **C:** `@tsparticles/shape-image` builds with zero GIF references
- [x] **C:** `@tsparticles/shape-gif` builds and registers as `type: "gif"`
- [x] **C:** GIF demo config (`utils/configs/src/g/gifs.ts`) works with new shape
- [x] **A:** Bundle impact decision recorded (engine vs plugin path) and within accepted threshold (Phase 14) — +183B minified, LOW, keep in engine
- [ ] **A:** Zero regressions on legacy background behavior
- [ ] **A:** Plugin extraction feasibility documented (future: `@tsparticles/plugin-background`)
- [ ] **H:** No NaN/Infinity in standard presets after 20k+ frame soak
- [ ] **H:** Fluid visual effect clearly distinguishable from plain attract/repulse
- [ ] **E:** Layer rendering order matches pre-layer order (visual regression test)
- [ ] **E:** Plugin with no `layer` set auto-detects correct layer
- [ ] **E:** Background element/draw render correctly as layer handlers
- [ ] **F:** No `particle.bubble` references remain in engine code
- [ ] **F:** Bubble hover/click works identically via modifier API
- [ ] **F:** Modifier add/remove lifecycle correct (particle.destroy clears all)
- [ ] **G:** HDR color pipeline uses float RGB (no Math.round)
- [ ] **G:** Non-HDR mode produces identical output to before
- [ ] **G:** Color animations in HDR show reduced banding (verify visually)

### Pre-Release Gate (run before cutting release branch)

- [ ] **ALL:** `pnpm run build:ci` passes
- [ ] **ALL:** No high-severity open issues targeting 4.3.0
- [x] **ALL:** Workspace `@tsparticles/` deps use `workspace:*` — automatico via `scripts/post-version-fix.js` (integrato in tutti i comandi `version:*`, corregge eventuali regressioni dopo lerna version e amenda lo stesso commit)
- [x] **ALL:** Root `package.json` version matches packages — automatico via `scripts/post-version-fix.js` (letto da `engine/package.json`, scritto nel root e incluso nel commit del tag)
- [ ] **ALL:** Integration tests pass (see below)
- [ ] **ALL:** Demo server smoke-tested (see below)

### Integration testing

After individual feature verification, run combined scenarios:

```bash
pnpm run build:ci
pnpm exec vitest run
```

Then smoke-test the demo server:

```bash
cd demo/vanilla && pnpm start
```

### Changelog entries (draft)

Each feature produces its own changelog section:

```
## 4.3.0

### New Features
- GIF Shape: new `@tsparticles/shape-gif` shape extracted from image shape.
  Use `type: "gif"` instead of `type: "image"` with `gif: true` (#C)
- Background Canvas: layered background pipeline (#A)
  - `background.element`: auto-draw external source (`HTMLCanvasElement`, `OffscreenCanvas`, `HTMLVideoElement`, `HTMLImageElement`)
  - `background.draw`: custom draw callback on main canvas context
  - Layer order: CSS → element auto-draw → draw callback → particles
- Background Mask Dynamic Source: `cover.element` and `cover.draw` for `@tsparticles/plugin-background-mask` (#D)
  - `cover.element`: auto-draw external source as mask (canvas, video, img, CSS selector)
  - `cover.draw`: custom draw callback for animated mask patterns
  - Dynamic layers skip static cover fallback when used
- Fluid Interaction: new `@tsparticles/interaction-particles-fluid` package with DDR-based
  liquid simulation, pointer stirring, and presets (#H)
- Draw Layer System: unified rendering pipeline with 8 named layers for explicit draw ordering (#E)
  - `DrawLayer` enum exported from engine
  - `IContainerPlugin` gains `layer?: DrawLayer` property
  - Background element/draw registered as internal layer handlers
- Particle Modifier System: generic `IParticleModifier` API for dynamic property overrides (#F)
  - `Particle.addModifier()`, `removeModifier()`, `clearModifiers()`
  - Bubble interactor uses modifier API instead of direct `particle.bubble.*` mutation
  - `IBubbleParticleData` removed from engine (moved to bubble package)
- HDR Precision Enhancement: floating-point RGB pipeline for display-p3 colors (#G)
  - `hslToRgbFloat()` — preserves precision by skipping `Math.round()`
  - HDR-aware random color generation (float RGB)
  - Higher cache key precision for HDR mode

### Breaking Changes
- `@tsparticles/shape-image`: removed animated GIF support (`gif` option no longer available).
  Migrate to `@tsparticles/shape-gif` with `type: "gif"`.
- `@tsparticles/engine`: `IBubbleParticleData` interface removed (moved to `@tsparticles/interaction-external-bubble`).
  External code referencing this type must import from the bubble package.
- `@tsparticles/engine`: `IContainerPlugin` plugins that implement drawing hooks (`draw`, `drawSettingsSetup`, etc.)
  should now declare a `layer` property for correct rendering order. Without it, the layer is auto-detected
  from implemented methods (backward compatible, but explicit is recommended).
- `@tsparticles/shape-image`: removed animated GIF support (`gif` option no longer available).
  Migrate to `@tsparticles/shape-gif` with `type: "gif"`.

### Bundle Changes
- `@tsparticles/all`: now includes `@tsparticles/shape-gif`
```

---

## Feature C — GIF Shape (from `GIF_SHAPE_PLAN.md`)

**Target:** `@tsparticles/shape-gif` (new), `@tsparticles/shape-image` (modified), `@tsparticles/all` (modified)
**Load function:** `loadGifShape(engine)`
**Plan substeps:** 1.1 through 6.1

### Implementation steps

| Step | Description | Verification |
|------|-------------|-------------|
| 1 | Scaffold `shapes/gif/` — 17 new files | Directory exists |
| 2 | Copy `GifUtils/` from image shape — adapt `drawGif`, remove `loadGifImage` | Imports resolve |
| 3 | Remove GIF from `@tsparticles/shape-image` — 9 files modified | `grep` zero matches |
| 4 | Add `@tsparticles/shape-gif` to `@tsparticles/all` bundle | Build passes |
| 5 | Build + verify — `pnpm run build:ci` | `dist/` output OK |
| 6 | Update demo config `utils/configs/src/g/gifs.ts` | Demo works |

### Critical files

| File | Change | Type |
|------|--------|------|
| `shapes/gif/src/GifDrawer.ts` | New — IShapeDrawer with GIF cache | New |
| `shapes/gif/src/GifUtils/Utils.ts` | Copied + adapted (drawGif, no loadGifImage) | Copy+edit |
| `shapes/image/src/Utils.ts` | Remove 7 gif fields + import | Modify |
| `shapes/image/src/ImageDrawer.ts` | Remove drawGif branch, gif fields | Modify |
| `bundles/all/src/index.ts` | Add loadGifShape import + call | Modify |

### User-facing config change

```jsonc
// Before (4.2.x):
{ "shape": { "type": "image", "options": { "image": { "gif": true, "src": "...", "width": 200, "height": 200 } } } }

// After (4.3.0):
{ "shape": { "type": "gif", "options": { "gif": { "src": "...", "width": 200, "height": 200 } } } }
```

---

## Feature A — Background Canvas (from `BACKGROUND_CANVAS_PLAN.md`)

**Target:** `@tsparticles/engine`
**Load function:** N/A (built into engine)

**Design:** Multi-layer background pipeline (CSS → element auto-draw → draw callback → particles).
`element` and `draw` are independent layers; element is auto-composited via `ctx.drawImage()`,
`draw` always receives the main canvas context. See `BACKGROUND_CANVAS_PLAN.md` for full details.

### Key decisions (from plan)

1. **Bundle impact gate** (Phase 14): re-measure delta after v2 refactor; if LOW/MEDIUM → keep in engine; if HIGH → extract `@tsparticles/plugin-background`
2. **Plugin extraction**: if engine grows too large, `@tsparticles/plugin-background` owns all programmable layers; engine retains only CSS-style background

### Implementation phases (v2 refactor)

| Phase | Description | Verification |
|-------|-------------|-------------|
| 9 | Types/methods rename — widen element type (`HTMLVideoElement \| HTMLImageElement`), rename `#backgroundContext` → `#backgroundElement` | Compile passes |
| 10 | Rewrite `#drawBackground()` — independent element auto-draw (`ctx.drawImage`) + draw callback on main context | Layering order verified |
| 11 | Rewrite `#resolveBackgroundContext()` → `#resolveBackgroundElement()` — store element ref, no `getContext("2d")` | Element stored, context not extracted |
| 12 | Update tests — remove element-as-context tests, add auto-draw + layering tests | Tests green |
| 13 | Update docs — layers, video/image examples, worker+OffscreenCanvas example | Docs match design |
| 14 | Bundle impact re-measurement + plugin extraction decision | Size delta block |
| 15 | Validation gate | Final checklist complete |

### Critical files

| File | Change |
|------|--------|
| `engine/src/Options/Interfaces/Background/IBackground.ts` | Widen `element` type to include `HTMLVideoElement \| HTMLImageElement` |
| `engine/src/Options/Classes/Background/Background.ts` | Update property type |
| `engine/src/Core/RenderManager.ts` | `#backgroundContext` → `#backgroundElement`; rewrite `#drawBackground()`; rewrite `#resolveBackgroundElement()` |
| `engine/src/export-types.ts` | Keep `BackgroundDrawContext` export (still used by `draw` callback) |

## Feature D — Background Mask Dynamic Source (from `BACKGROUND_MASK_DYNAMIC_PLAN.md`)

**Target:** `@tsparticles/plugin-background-mask`
**Load function:** N/A (plugin già esistente, no nuovo loader)

**Design:** Aggiungere `element` (auto-draw via `ctx.drawImage()`) e `draw` (callback personalizzato) al `cover` del BackgroundMask, replicando il pattern layered già validato in BACKGROUND_CANVAS_PLAN v2. Se `element` o `draw` sono impostati, la copertura statica (color/image) viene skippata. Il contenuto dinamico viene disegnato durante `canvasPaint()`, prima che il composite mode `destination-out` ritagli le particelle.

**Priorità:** Più alta della Fluid Interaction (H).

### Implementation phases

| Phase | Description | Verification |
|-------|-------------|-------------|
| 0 | Baseline scan + pattern reference da BACKGROUND_CANVAS_PLAN | Shortlist copied codice engine → plugin |
| 1 | Type contract — `element` + `draw` in `IBackgroundMaskCover` + `BackgroundMaskCover` | Compile passes |
| 2 | Runtime hook — modificare `canvasPaint()` per layer dinamici + fallback legacy | Dynamic mask funzionante |
| 3 | Element resolution — `#resolveMaskElement()` copiata/adattata da engine | CSS selector + direct ref funzionano |
| 4 | Error/warning handling — 4 warning keys, once-per-container | Nessun log spam |
| 5 | Tests — opzioni, risoluzione, canvasPaint layers, error resilience | Tests green |
| 6 | Bundle impact + docs — README aggiornato con esempi | Docs completate |
| 7 | Validation gate — build + test + manual smoke | Checklist completa |

### Critical files

| File | Change |
|------|--------|
| `plugins/backgroundMask/src/Options/Interfaces/IBackgroundMaskCover.ts` | Aggiungere `element` + `draw` |
| `plugins/backgroundMask/src/Options/Classes/BackgroundMaskCover.ts` | Aggiungere proprietà + `load()` |
| `plugins/backgroundMask/src/BackgroundMaskPluginInstance.ts` | `#maskElement`, `#resolveMaskElement()`, mod `canvasPaint()`, warning, init/destroy |

Nessun cambiamento all'engine.

---

## Feature E — Draw Layer System (from `DRAW_LAYER_SYSTEM_PLAN.md`)

**Target:** `@tsparticles/engine`
**Load function:** N/A (built into engine)
**Execution note:** Implement AFTER Feature A is committed (both touch RenderManager.ts).

**Design:** Replace RenderManager's 11 separate plugin arrays with a unified layer system using a fixed set of 8 named layers (`DrawLayer` enum). Each layer has a fixed ordinal position. Plugins declare their layer via `IContainerPlugin.layer`. Background element and draw become internal handlers registered on `BackgroundElement`/`BackgroundDraw` layers.

### Implementation phases

| Phase | Description | Verification |
|-------|-------------|-------------|
| 0 | `DrawLayer` enum in `engine/src/Core/Enums/DrawLayer.ts`; add `layer?: DrawLayer` to `IContainerPlugin`; exports | Compile passes |
| 1 | Replace 11 arrays with `#layers: IContainerPlugin[][]`; rewrite `initPlugins()` and `drawParticles()`; split `#drawBackground()` into layer handlers | Build passes, render matches |
| 2 | Plugin migration: built-in plugins set explicit `layer` | Grep shows no unmigrated drawing plugins |
| 3 | Tests + docs | Tests green |

### Critical files

| File | Change |
|------|--------|
| `engine/src/Core/Enums/DrawLayer.ts` | **New** — enum with 8 layers |
| `engine/src/Core/RenderManager.ts` | Major refactor: layers, initPlugins, drawParticles |
| `engine/src/Core/Interfaces/IContainerPlugin.ts` | Add `layer?: DrawLayer` |
| `plugins/backgroundMask/src/BackgroundMaskPluginInstance.ts` | Set explicit layer |
| `plugins/trail/src/TrailPluginInstance.ts` | Set explicit layer |

---

## Feature F — Particle Modifier System (from `PARTICLE_MODIFIER_PLAN.md`)

**Target:** `@tsparticles/engine` + `@tsparticles/interaction-external-bubble`
**Load function:** N/A

**Design:** Replace `particle.bubble` with a generic `IParticleModifier` API. Any plugin can add/remove modifiers on a particle to override radius, opacity, fill/stroke color. Bubble interactor becomes a consumer of this API instead of directly mutating particle state.

### Implementation phases

| Phase | Description | Verification |
|-------|-------------|-------------|
| 0 | `IParticleModifier` interface in `engine/src/Core/Interfaces/IParticleModifier.ts`; exports | Compile passes |
| 1 | Add `#modifiers` array + `addModifier`/`removeModifier`/`clearModifiers` to `Particle`; refactor `getFillColor`, `getOpacity`, `getRadius`, `getStrokeColor` to use modifiers; remove `bubble` property | Build passes, no `particle.bubble` in engine |
| 2 | Migrate `Bubbler.ts` to use modifier API; move `IBubbleParticleData` to bubble package | Bubble works identically |
| 3 | Cleanup: destroy/modifier lifecycle, grep for stale bubble refs | All tests green |

### Critical files

| File | Change |
|------|--------|
| `engine/src/Core/Interfaces/IParticleModifier.ts` | **New** — Modifier interface |
| `engine/src/Core/Particle.ts` | Add modifier methods, refactor getters, remove `bubble` |
| `engine/src/export-types.ts` | Export `IParticleModifier` |
| `engine/src/Core/Interfaces/IBubbleParticleData.ts` | **Remove** from engine |
| `interactions/external/bubble/src/Bubbler.ts` | Use modifier API |
| `interactions/external/bubble/src/IBubbleParticleData.ts` | **New** (plugin copy) |

---

## Feature G — HDR Precision (from `HDR_PRECISION_PLAN.md`)

**Target:** `@tsparticles/engine`
**Load function:** N/A

**Design:** When HDR is active, skip the integer rounding step in `hslToRgb()` to preserve floating-point precision through the display-p3 pipeline. Add higher-precision cache keys and HDR-aware random color generation.

### Implementation phases

| Phase | Description | Verification |
|-------|-------------|-------------|
| 0 | Add HDR precision constants to `Constants.ts` (`hdrRgbPrecision`, `hdrHslPrecision`, etc.) | Compile passes |
| 1 | Create `hslToRgbFloat()` (no `Math.round`); export | Tests pass |
| 2 | Update `getStyleFromHsl` to use `hslToRgbFloat` when HDR; increase cache precision in HDR; add float RGB generation for `getRandomRgbColor` in HDR | HDR shows no banding |
| 3 | Verify non-HDR output identical; verify color animation smoothness | No regression |

### Critical files

| File | Change |
|------|--------|
| `engine/src/Utils/ColorUtils.ts` | `hslToRgbFloat()`, update `getStyleFromHsl`, `getRandomRgbColor`, cache precision |
| `engine/src/Core/Utils/Constants.ts` | Add HDR precision constants |
| `engine/src/export-types.ts` | Export `hslToRgbFloat` |

---

## Feature H — Fluid Interaction (from `FLUID_INTERACTION_PLAN.md`)

**Target:** `@tsparticles/interaction-particles-fluid`
**Load function:** `loadParticlesFluidInteraction(engine)`
**Plan steps:** 0-9
**Status:** 🕐 in attesa di revisione — lowest priority, implementare solo se approvato

### Implementation steps

| Step | Description | Verification |
|------|-------------|-------------|
| 0 | Repository alignment — inspect conventions, freeze naming | Checklist |
| 1 | Package skeleton — scaffold + noop `FluidInteractor` | Build passes |
| 2 | Options contract — `IFluid` + classes + loaders + unit tests | Tests pass |
| 3 | Baseline DDR solver — density, pressure, displacement, velocity reconstruction | Visual blob, no NaN |
| 4 | Constraints — boundary clamp/bounce, maxNeighbors, stability | Stable at edges |
| 5 | Viscosity + pointer stirring | Local deformation + recovery |
| 6 | Interoperability — validate with move/emitters/repulse/collisions | Matrix validated |
| 7 | Performance + soak — benchmark 300/600/1000 particles | Meets thresholds |
| 8 | Docs + presets — README, 3 presets, changelog | No undocumented options |
| 9 | Optional advanced path (plugin orchestration) — only if ordering artifacts | Before/after evidence |

### Critical options (defaults)

```json
{
  "particles": {
    "fluid": {
      "enable": false,
      "radius": 34, "stiffness": 0.45, "nearStiffness": 0.8,
      "restDensity": 3, "viscosity": 0.08, "iterations": 2,
      "maxForce": 2.5, "maxNeighbors": 64,
      "boundaryBounce": 0.2, "boundaryClamp": true,
      "gravity": { "enable": true, "acceleration": 0.01 },
      "pointer": { "enable": false, "radius": 90, "strength": 0.45, "mode": "push" }
    }
  }
}
```

---

## Release Infrastructure

### Root `package.json` version sync

✅ **Risolto nella 4.2.1** — integrato in `scripts/post-version-fix.js`.

`post-version-fix.js` viene eseguito da `pnpm run postversion:fix` dopo ogni `lerna version`:
1. Legge `engine/package.json` → `version`
2. Se il root `package.json` è diverso, lo sovrascrive
3. `git add -A && git commit --amend --no-edit` lo include nello stesso commit del tag

---

## Rollback Plan

If any feature is not ready at the 4.3.0 freeze date:

| Scenario | Action |
|----------|--------|
| D not ready | Ship C + A + E + F + G as 4.3.0, defer D to 4.4.0 |
| C not ready | Ship D + A + E + F + G as 4.3.0, defer C to 4.4.0 |
| A not ready | Ship C + D + F + G as 4.3.0 (E blocked by A; defer A+E to 4.4.0) |
| E not ready | Ship C + D + A + F + G as 4.3.0, defer E to 4.4.0 |
| F not ready | Ship C + D + A + E + G as 4.3.0, defer F to 4.4.0 |
| G not ready | Ship C + D + A + E + F as 4.3.0, defer G to 4.4.0 |
| A+E not ready | Ship C + D + F + G as 4.3.0, defer A+E to 4.4.0 |
| D+C+A not ready | Ship F + G as 4.3.0 |
| H not ready | Ship D + C + A + E + F + G as 4.3.0 (H deferita a 4.4.0, baseline) |
| All not ready | No 4.3.0 — reassess scope |

Each feature's branch must be independent so partial shipping is trivial.


