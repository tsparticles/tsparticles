# tsParticles 4.5.0 — Fluid Interaction, MCP `generate_code` & Emitter Draw/Drag Release Plan

## Status

4.5.0 scope is now defined as:

1. **Fluid particle behavior via collisions mode** — the DDR-based liquid simulation moved from the scrapped 4.4.0 v3 attempt to the v5 "fluid-as-collision-mode" design.
2. **MCP server `generate_code` tool** — a deterministic code generator tool for `@tsparticles/mcp-server`.
3. **Emitter draw & drag** — emitters can be rendered on the canvas and dragged with the mouse, mirroring the absorber plugin capabilities.

HDR color fixes and code review fixes shipped in 4.4.0. See `4.4.0_PLAN.md`.

Primary technical sources: `FLUID_INTERACTION_PLAN.md`, `MCP_GENERATE_CODE_PLAN.md`, `EMITTERS_DRAW_DRAG_PLAN.md`.

---

## Release Feature Inventory

| # | Feature | Package | Type | Plan |
|---|---------|---------|------|------|
| A | Fluid particle behavior via collisions mode | `@tsparticles/interaction-particles-collisions` | Existing package extension | `FLUID_INTERACTION_PLAN.md` |
| B | MCP `generate_code` tool (deterministic codegen) | `@tsparticles/mcp-server` | New tool | `MCP_GENERATE_CODE_PLAN.md` |
| C | Emitter draw & drag | `@tsparticles/plugin-emitters` (+ `emittersShapes/*`) | Existing package extension | `EMITTERS_DRAW_DRAG_PLAN.md` |

---

## High-Level Goal

Ship a stable `particles.collisions.mode = "fluid"` behavior that:

- uses DDR-based neighborhood resolution (correct `q` factor + velocity reconstruction)
- remains mutually exclusive with `bounce` / `absorb` / `destroy`
- requires no new plugin package or loader
- preserves backward compatibility for existing collision modes

and add a deterministic `generate_code` MCP tool that:

- turns a natural language description into complete, ready-to-use code
- favors specialized bundles (`@tsparticles/confetti`, `@tsparticles/fireworks`, etc.)
- stays a pure function (no filesystem I/O, no network, no LLM dependency)

and extend the emitter plugin so that:

- emitters can be drawn on the canvas (shape rendering, toggle via `draw`)
- emitters can be dragged with the mouse while continuing to emit (toggle via `draggable`)

---

## Scope Definition

## In scope

1. Fluid configured through `particles.collisions.mode = "fluid"` in the collisions interaction package
2. `collisions.fluid` options contract, defaults, and documentation
3. MCP `generate_code` tool under `src/tools/` + integration in `src/index.ts`
4. Emitter `draw` option + per-shape `draw()` and `EmittersPluginInstance.draw()` rendering
5. Emitter `draggable` option + drag handling in `EmittersInteractor` with `setPosition()` on `EmitterInstance`
6. FAQ: no new `@tsparticles/interaction-particles-fluid` package or loader for feature A

## Out of scope

1. Standalone fluid package `@tsparticles/interaction-particles-fluid` (rejected — fluid is a collision mode)
2. Anything already shipped in 4.4.0 (HDR fixes, wrapper fixes)
3. Emitter drag for `domId`-linked emitters (position is DOM-derived)

---

## Feature A — Fluid as Collision Mode

### Phase 0 — Baseline and guardrails

1. Validate current collisions package build + tests baseline
2. Record current behavior of `bounce`, `absorb`, `destroy` for regression reference
3. Confirm planning docs no longer depend on standalone fluid package

Exit criteria:

- baseline green
- migration target clear (`collisions.mode = "fluid"`)

### Phase 1 — Contract extension (types + options)

1. Add `fluid` to `CollisionMode`
2. Add `ICollisionsFluid` interface and `CollisionsFluid` class with defaults/load
3. Wire `collisions.fluid` loading in collisions options class
4. Ensure defaults are present and safe

Exit criteria:

- options parse correctly
- type surface compiles

### Phase 2 — Runtime implementation

1. Extend collision particle runtime with fluid transient state (incl. `prevPos`)
2. Implement pre-advection snapshot in `Collider.reset()` for fluid-mode particles
3. Implement fluid DDR branch in `Collider.interact()`:
   - density and near-density accumulation
   - pressure computation
   - soft boundary spring + hard boundary clamp
   - symmetric displacement (`dir * q * (P + Pnear * q) * 0.5`)
   - velocity reconstruction from `prevPos`
4. Keep classic collision branch untouched

Exit criteria:

- fluid mode runs without NaN/Infinity
- classic modes behavior unchanged by branch isolation

### Phase 3 — Stabilization and tuning

1. Validate behavior on representative configs (baseline/soft/dense)
2. Verify no mixed-mode artifacts
3. Add guards/clamps/neighbor caps
4. Check performance characteristics with medium/high particle counts

Exit criteria:

- stable visual behavior (cohesive pooling with gravity)
- acceptable performance profile, no hot-loop allocations

### Phase 4 — Documentation and release readiness

1. Update collisions markdown docs (`mode` table + `collisions.fluid` options)
2. Ensure release notes describe fluid as collisions extension
3. Verify no docs mention a standalone fluid package/loader as an active path

Exit criteria:

- docs coherent with implementation
- release notes aligned with product decision

---

## Feature B — MCP `generate_code` Tool

> **Status: implemented** — `generate_code` is live in `integrations/mcp-server` (tool +
> validation + 44 tests). See `MCP_GENERATE_CODE_PLAN.md` for implementation notes and the
> marked checklist. Remaining: CLI lint target is not configured for this package (n/a), manual
> MCP-client verification optional.

### Phase 0 — Baseline

1. [x] Confirm current `@tsparticles/mcp-server` tool listing and build/test baseline
2. [x] Verify bundle registry/catalog files are read-only imports

Exit criteria:

- server build + existing tests green

### Phase 1 — Types and validation

1. [x] Add `GenerateCodeInput`, `GenerateCodeOutput`, `BundleMatch` to `src/types.ts`
2. [x] Add `generateCodeArgsSchema` to `src/validation.ts`

Exit criteria:

- schemas compile and validate

### Phase 2 — Core logic

1. [x] Create `src/tools/generateCode.ts` with keyword extraction
2. [x] Implement `matchBundle()` with priority-ordered keyword table (specialized bundles first)
3. [x] Implement `generateOptions()` with templates + overrides
4. [x] Implement framework codegen for all 5 frameworks
5. [x] Implement HTML generation
6. [x] Implement install command generation
7. [x] Wire `suggestPlugins()` for validation

Exit criteria:

- deterministic output for representative descriptions

### Phase 3 — MCP integration

1. [x] Import and register in `src/index.ts` (ListTools + CallTool)
2. [x] Verify tool appears in MCP tool listing

Exit criteria:

- tool discoverable and callable

### Phase 4 — Tests

1. [x] Create `src/tools/generateCode.test.ts`
2. [x] Bundle selection tests (13+ cases), options generation tests (5+ cases)
3. [x] Framework codegen tests (5 frameworks × 2 TS/JS)
4. [x] Integration tests (3+ cases)
5. [x] Run `pnpm exec vitest` — all pass (146 tests across mcp-server)
6. [x] Lint + build via `pnpm nx run @tsparticles/mcp-server:lint` / `:build` (build green; no lint target configured)

Exit criteria:

- >90% branch coverage for `generateCode.ts`
- full suite green

### Phase 5 — Documentation

1. [x] Update `README.md` tools table
2. [x] Add `generate_code` and `diagnose_issues` to README tools table (currently missing)

Exit criteria:

- docs reflect all available tools

---

## Feature C — Emitter Draw & Drag

> **Status: planned** — full implementation detail in `EMITTERS_DRAW_DRAG_PLAN.md`. Two independent
> capabilities mirroring the absorber plugin: shape rendering on canvas (`draw`) and mouse dragging
> (`draggable`). Both features already exist in absorbers, so this is a port/follow of that pattern.

### Phase 0 — Baseline

1. Confirm emitter/emittersShapes build + lint baseline
2. Record absorber draw/drag reference behavior (already implemented, used as template)

Exit criteria:

- baseline green

### Phase 1 — Options contract

1. Add `draw: boolean` (default `true`) and `draggable: boolean` (default `false`) to `IEmitter`/`Emitter`
2. Load both via `loadProperty` in `Emitter.load()`

Exit criteria:

- options parse correctly, type surface compiles

### Phase 2 — Drawing

1. Add `draw(context)` to `IEmitterShape`/`EmitterShapeBase`
2. Implement in `emittersShapes/{square,circle,polygon,path,canvas}` (`canvas` may be a no-op in first pass)
3. Add `EmitterInstance.draw()` using spawn fill/stroke colors (default black fill)
4. Add `EmittersPluginInstance.draw()` looping emitter instances

Exit criteria:

- emitters render behind particles; `draw: false` keeps them invisible

### Phase 3 — Dragging

1. Add `EmitterInstance.setPosition()` keeping shape position in sync
2. Add drag state + rectangular hit-test in `EmittersInteractor.interact()`
3. Exclude `domId`-linked emitters from dragging

Exit criteria:

- draggable emitters move with the mouse while emitting; release stops the drag

### Phase 4 — Documentation & release readiness

1. Update `markdown/Options/Plugins/Emitters.md` with `draw` and `draggable`
2. Add release notes entries (see Changelog Entry draft below)

Exit criteria:

- docs coherent with implementation, release notes include emitter feature

---

## Detailed Task Matrix

| Area | Tasks |
|------|-------|
| Enums | Add `fluid` value to `CollisionMode` |
| Options | Add `ICollisionsFluid` + `CollisionsFluid` + loader wiring |
| Runtime | Extend `CollisionParticle` state, implement fluid branch and reset snapshot in `Collider` |
| Resolver | Keep pair resolver for classic modes only (`bounce`/`absorb`/`destroy`) |
| MCP server | `src/types.ts`, `src/validation.ts`, `src/tools/generateCode.ts`, `src/index.ts` |
| Tests | Collisions unit + regression + visual; MCP bundle/options/framework codegen tests |
| Docs | Collisions fluid mode docs; MCP server README tools table |
| Emitter options | Add `draw` + `draggable` to `IEmitter`/`Emitter` with `loadProperty` |
| Emitter shapes | Add `draw(context)` to `IEmitterShape`/`EmitterShapeBase` and implement in `emittersShapes/{square,circle,polygon,path,canvas}` |
| Emitter drawing | `EmitterInstance.draw()` (styles from spawn fill/stroke) + `EmittersPluginInstance.draw()` |
| Emitter dragging | `EmitterInstance.setPosition()` + drag state/hit-test in `EmittersInteractor` |
| Emitter docs | `markdown/Options/Plugins/Emitters.md` (`draw`, `draggable`) |

---

## Risks and Mitigation

### Risk 1 (Fluid): classic mode regression

Mitigation:

- strict mode branching
- targeted regression tests for `bounce`, `absorb`, `destroy`

### Risk 2 (Fluid): fluid instability (jitter/explosion)

Mitigation:

- enforce formula correctness (`dir * q * (P + Pnear * q) * 0.5`)
- force clamp, distance epsilon guards
- velocity reconstruction from `prevPos`
- conservative defaults

### Risk 3 (Fluid): boundary artifacts

Mitigation:

- soft spring + hard clamp sequence in fluid branch
- documented recommendation for `move.outModes`

### Risk 4 (Fluid): performance drop on dense scenes

Mitigation:

- neighbor cap
- grid-based queries
- avoid hot-loop allocations

### Risk 5 (MCP): free-form descriptions produce unusable output

Mitigation:

- deterministic keyword matching with priority-ordered bundles
- framework-specific templates with overrides
- comprehensive bundle/options/codegen tests

### Risk 6 (Emitters): drawing changes visual behavior for existing users

Mitigation:

- gate behind explicit `draw` option (default `true` for parity with absorbers; can ship
  default `false` to preserve current invisible-emitter behavior if the team prefers)
- `context.save()/restore()` to avoid state leakage into particle rendering

### Risk 7 (Emitters): drag fights DOM-linked emitters or `resize()`

Mitigation:

- exclude `domId`-linked emitters
- `setPosition()` updates position + shape without recomputing size from options
- rectangular half-width/half-height hit-test (emitters are not circular like absorbers)

---

## Verification Plan

## Build and test

- `pnpm nx build interaction-particles-collisions`
- collisions package tests (existing + new fluid tests)
- `pnpm exec vitest` — full suite (MCP + collisions + emitters)
- `pnpm nx run @tsparticles/mcp-server:lint` and `:build`
- `pnpm --filter @tsparticles/plugin-emitters run lint` and `run build`
- build each `@tsparticles/plugin-emittersShapes-*` package (square, circle, polygon, path, canvas)

## Behavioral verification

1. `mode = "fluid"` produces cohesive pooling with gravity
2. `mode = "bounce"` remains elastic and unaffected
3. switching mode changes behavior cleanly without overlap semantics leakage
4. `generate_code({ description: "confetti", framework: "react" })` returns specialized-bundle + copy-pasteable code
5. Specialized bundles are NEVER bypassed for matching descriptions
6. Emitter with `draw` renders its shape behind particles; `draw: false` keeps it invisible
7. Emitter with `draggable` is grabbable inside its bounds and keeps emitting while dragged; `draggable: false` and `domId` emitters are never draggable

## Documentation verification

1. `markdown/Options/Particles/Collisions.md` includes fluid mode
2. options examples use `particles.collisions.fluid`
3. no active docs require `@tsparticles/interaction-particles-fluid`
4. MCP server README lists `generate_code` and `diagnose_issues`
5. `markdown/Options/Plugins/Emitters.md` documents `draw` and `draggable`

---

## Release Gate Checklist

- [ ] Collisions build passes
- [ ] Collisions tests pass (fluid + classic regression)
- [x] MCP server build/tests pass (146 tests green; no lint target configured for this package)
- [x] MCP `generate_code` tests pass (44 tests in `generateCode.test.ts` incl. >90% branch coverage)
- [ ] Fluid visual validation with recommended config
- [x] Docs updated and consistent (README tools table + planning docs)
- [ ] Release note snippet updated
- [ ] Emitters plugin + all emittersShapes packages build/lint pass
- [ ] Emitters draw/drag validation in demo (canvas rendering + mouse drag)
- [ ] Emitters docs updated (`draw`, `draggable`)

---

## Acceptance Criteria

### Feature A (Fluid)

1. Fluid is configured through `particles.collisions.mode = "fluid"`.
2. Fluid tuning lives under `particles.collisions.fluid`.
3. No standalone fluid package/interactor/loader is introduced.
4. Fluid and classic collision modes are mutually exclusive at runtime.
5. Existing collision modes remain stable and backward compatible.
6. Fluid mode produces observable cohesive pooling with recommended config.

### Feature B (MCP)

7. `generate_code` is callable with `{ description: "confetti" }` and returns valid output.
8. `@tsparticles/confetti` is always selected for confetti descriptions (EN + IT); same for fireworks.
9. Specialized bundles are never bypassed in favor of generic bundles when keywords match.
10. Generated code compiles/runs without modification for vanilla + each framework.
11. Install command includes all required packages.
12. All existing tests pass without modification.

### Feature C (Emitter draw & drag)

13. Emitters render their shape on the canvas behind particles when `draw` is enabled.
14. `draw: false` keeps emitters invisible (legacy behavior preserved).
15. Draggable emitters can be grabbed inside their bounds and dragged while continuing to emit.
16. `draggable: false` and `domId`-linked emitters are never draggable.
17. `draw` and `draggable` load correctly through the config API (JSON string + object form).

---

## Changelog Entry (draft)

```markdown
## 4.5.0

### New Features
- Collisions: added `particles.collisions.mode = "fluid"` for DDR-based liquid-like particle behavior.
  - Configure via `particles.collisions.fluid` options.
  - Implemented in the existing collisions interaction package (no new package/loader).
- MCP server: added deterministic `generate_code` tool producing ready-to-use, framework-specific code
  with smart bundle selection (specialized bundles preferred).
- Emitters: added `draw` option to render the emitter shape on the canvas (filled with the spawn
  fill color, stroked with the spawn stroke color when provided).
- Emitters: added `draggable` option to move emitters with the mouse while they keep emitting.
```

---

## Rollback Strategy

### Fluid

1. Keep enum/options behind internal state or revert fluid additions
2. Ship 4.5.0 without fluid mode if stability/performance criteria are unmet
3. Preserve the fluid-as-collisions-mode architectural decision for next iteration

### MCP `generate_code`

1. Keep the tool behind the existing `src/tools/` structure; it is additive-only
2. If codegen quality is inadequate, do not expose it in MCP `ListTools` and fix in follow-up

### Emitter draw & drag

1. Additive-only options (`draw`, `draggable`); removing them restores 4.4.0 behavior
2. If drawing causes visual regressions, ship with `draw` defaulting to `false` in 4.5.0
3. Dragging is isolated in `EmittersInteractor` and gated by mode enable states — trivially removable