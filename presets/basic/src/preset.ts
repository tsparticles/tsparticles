import { tsParticles } from "tsparticles";

tsParticles.addPreset("basic", {
    particles: {
        lineLinked: {
            enable: true,
        },
        move: {
            enable: true,
        },
    },
});
