import { loadConfettiCannonPreset, confetti } from "./preset";
import { tsParticles } from "tsparticles";

loadConfettiCannonPreset(tsParticles);

export { confetti, loadConfettiCannonPreset, tsParticles };
