/** Destroy type values for particle destruction */
export enum DestroyType {
  /** No destruction */
  none = "none",
  /** Destroy when reaching the maximum value */
  max = "max",
  /** Destroy when reaching the minimum value */
  min = "min",
}
