/** Particle out type values */
export enum ParticleOutType {
  /** Normal out behavior */
  normal = "normal",
  /** Particle is inside the canvas */
  inside = "inside",
  /** Particle is outside the canvas */
  outside = "outside",
}
