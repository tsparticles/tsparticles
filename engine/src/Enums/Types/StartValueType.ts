/** Start value types for animations */
export enum StartValueType {
  /** Start from the maximum value */
  max = "max",
  /** Start from the minimum value */
  min = "min",
  /** Start from a random value */
  random = "random",
}
