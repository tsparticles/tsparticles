/** Out mode direction values for particles leaving the canvas */
export enum OutModeDirection {
  /** Out mode applied to the bottom edge */
  bottom = "bottom",
  /** Out mode applied to the left edge */
  left = "left",
  /** Out mode applied to the right edge */
  right = "right",
  /** Out mode applied to the top edge */
  top = "top",
}
