/** Pixel mode values for coordinate and size interpretation */
export enum PixelMode {
  /** Values are treated as exact pixel values */
  precise = "precise",
  /** Values are treated as percentages */
  percent = "percent",
}
