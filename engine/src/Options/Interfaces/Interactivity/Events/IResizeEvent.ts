export interface IResizeEvent {
    /**
     * This property is used to define the delay between resize events.
     */
    delay: number;

    /**
     * This property is used to enable or disable the resize event.
     */
    enable: boolean;
}
