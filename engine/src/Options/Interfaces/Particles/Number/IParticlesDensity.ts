/**
 * @category Options
 */
export interface IParticlesDensity {
    /**
     * @deprecated use the new width/height values instead
     */
    area: number;

    enable: boolean;

    /**
     * @deprecated use the new width/height instead
     */
    factor: number;

    height: number;

    /**
     * @deprecated use the new area instead
     */
    value_area: number;

    width: number;
}
