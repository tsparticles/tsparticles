import type { RangeValue } from "tsparticles-engine";

/**
 * @category Options
 */
export interface IRotateAnimation {
    decay: RangeValue;
    enable: boolean;
    speed: RangeValue;
    sync: boolean;
}
