# Formas do playground

Use esta pagina para explorar valores de `shape.type` de forma isolada, com uma configuracao focada por forma e `shape.options` explicitas quando a forma as suporta.

<PlaygroundPanel mode="shapes"></PlaygroundPanel>
