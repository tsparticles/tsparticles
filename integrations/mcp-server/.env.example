# Port the MCP server listens on (default: 3000)
SERVER_PORT=3000

# Optional bearer token required on every /mcp request
# (Authorization: Bearer <token>). Strongly recommended when exposing
# the server beyond localhost (tunnel, reverse proxy, etc).
MCP_AUTH_TOKEN=
