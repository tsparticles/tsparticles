{
  "private": true,
  "name": "tsparticles",
  "version": "0.0.0",
  "scripts": {
    "publish:alpha": "lerna publish prerelease --pre-dist-tag alpha --preid alpha",
    "publish:beta": "lerna publish prerelease --pre-dist-tag beta --preid beta"
  },
  "workspaces": [
    "core/*",
    "demo/*",
    "presets/*",
    "shapes/*",
    "wrappers/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^8.3.5",
    "@commitlint/config-conventional": "^8.3.4",
    "husky": "^4.2.5",
    "lerna": "^3.22.1"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
